# test-docker

testing my first docker project (Prcatice)

# Run docker command

docker run -p 3000:3000 --name nodeapp testnode

port binding = -p 3000:3000
container name = --name nodeapp
image name = testnode
